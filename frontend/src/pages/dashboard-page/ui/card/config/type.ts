export type AuthorType = {
  avatarUrl?: string | null;
  username: string;
}

export type ExecutorType = {
  avatarUrl?: string | null;
  username: string;
}

export type CardType = {
  title: string;
  description: string;
}